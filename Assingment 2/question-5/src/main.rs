fn main() {
    println!("Rust’s closures are anonymous functions you can save in a variable or pass as arguments to other functions");
    println!("Closures, a function-like construct you can store in a variable");
    println!("You can create the closure in one place and then call the closure to evaluate it in a different context.");
    println!("Unlike functions, closures can capture values from the scope in which they’re defined");

}